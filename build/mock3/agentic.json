[
 {
  "scenarioTitle": "Claude Code for Continuous Integration",
  "domain": "Agentic Architecture",
  "prompt": "Your PR reviewer runs with <code>claude -p</code> and, besides posting findings, has permission to commit fixes directly to the PR branch. Last sprint 3 of 40 auto-commits changed behaviour: one \"fix\" for a flagged null check removed an intentional guard, and the author never noticed because the commit landed under the bot's name mid-review. You want to keep offering fixes. What design change addresses this?",
  "options": [
   {
    "letter": "A",
    "text": "Keep autonomous commits but require the reviewer to run the full test suite first and commit only when it is green, treating a passing run as the approval signal.",
    "correct": false,
    "explanation": "The removed guard already passed the suite, so a green run is not evidence that behaviour was preserved. Tests approve what they cover, not the author's intent; this keeps the bot deciding on its own and adds CI time without closing the gap that let the change through."
   },
   {
    "letter": "B",
    "text": "Have the reviewer post fixes as suggested-change blocks the author applies, and reserve committing for an explicit opt-in trigger such as a comment on the PR.",
    "correct": true,
    "explanation": "This keeps the value of generated fixes while moving the decision to the author: a suggestion is visible, attributable, and applied deliberately. An opt-in trigger still allows autonomous commits where the author wants them. It beats the other options because it bounds autonomy at the point where intent is unknowable to the bot."
   },
   {
    "letter": "C",
    "text": "Move fix generation to a separate subagent with a narrower prompt so the findings and the code changes come from independent contexts.",
    "correct": false,
    "explanation": "Independent contexts improve review quality, but the problem here is who decides, not how the fix was derived. A separate subagent would still commit a behaviour-changing fix under the bot's name with the author unaware; it adds cost and does not change the autonomy boundary."
   },
   {
    "letter": "D",
    "text": "Restrict the bot to committing only findings rated \"low\" severity, since higher-severity changes deserve author attention and low ones are safe.",
    "correct": false,
    "explanation": "Severity describes the impact of the finding, not the risk of the fix. A null-check cleanup is exactly the kind of \"low\" finding whose fix silently changed behaviour. Tying autonomy to severity would let the same class of commits continue while making the rule feel principled."
   }
  ]
 },
 {
  "scenarioTitle": "Claude Code for Continuous Integration",
  "domain": "Agentic Architecture",
  "prompt": "Your reviewer runs after a <code>gitleaks</code> step. Last week a PR added an internal service token in a format the scanner has no rule for. The reviewer noticed it, posted an inline comment quoting the value with the advice \"move this to an environment variable\", and continued with fourteen style findings. The comment was visible to every member of the GitHub org for two days before anyone rotated the token. How should the reviewer behave when it finds a live credential?",
  "options": [
   {
    "letter": "A",
    "text": "Add \"never quote secret values in comments\" to the review prompt and keep the finding in the normal list, with severity set to critical so it stands out.",
    "correct": false,
    "explanation": "A prompt rule reduces the chance of echoing the secret but keeps the event inside the ordinary review flow: a critical finding among fourteen style notes waits for the author to read it, and nobody who can rotate the token is told. The exposure clock keeps running."
   },
   {
    "letter": "B",
    "text": "Add the internal token pattern to the <code>gitleaks</code> rules so the scanner blocks such PRs before the reviewer runs, and leave the reviewer's behaviour as it is.",
    "correct": false,
    "explanation": "Extending the scanner is worth doing, but it only covers formats someone has already written a rule for; the stem is about the case the scanner missed. It leaves the reviewer with the same behaviour the next time it is the only component that notices a leak."
   },
   {
    "letter": "C",
    "text": "Have the reviewer redact the value and post the finding as critical severity so the existing severity gate blocks the merge until the author removes it.",
    "correct": false,
    "explanation": "Redaction and a merge block are improvements, yet a pushed credential is compromised whether or not the PR merges. Blocking the merge is the wrong control for the actual risk, and the security team still learns about it only if the author acts."
   },
   {
    "letter": "D",
    "text": "Treat it as a hard stop: halt the review, fail the check with a generic message, and trigger the security escalation path without echoing the value.",
    "correct": true,
    "explanation": "A live credential is a finding where continuing the task is the wrong decision: the review's value is negligible next to the exposure, and the right recipient is the security on-call, not the PR author. Stopping, failing the check, and escalating through a defined path without echoing the value is the stop condition the agent needs; completing the review first is what caused the two-day delay."
   }
  ]
 },
 {
  "scenarioTitle": "Claude Code for Continuous Integration",
  "domain": "Agentic Architecture",
  "prompt": "Your reviewer meets a PR that changes the retry count for payment webhooks from 5 to 2. No ticket is linked, the description says \"cleanup\", and neither diff nor tests reveal whether the change is intentional. The prompt currently forces every observation into a severity-rated finding, so this would post as \"critical: reduced resilience\" and block the merge. How should the reviewer handle observations where intent cannot be determined?",
  "options": [
   {
    "letter": "A",
    "text": "Post it as a question to the author citing the evidence, kept separate from defect findings and excluded from severity-based merge gates.",
    "correct": true,
    "explanation": "An unexplained behaviour change is worth surfacing, but as a question, not a verdict. Separating questions from defects keeps severity meaningful for gating while still getting the author to confirm intent. This beats suppressing it, mislabelling it, or spending turns trying to force a classification the evidence cannot support."
   },
   {
    "letter": "B",
    "text": "Suppress it: if the reviewer cannot establish a defect, posting anything adds to false positives and erodes trust in the reviewer.",
    "correct": false,
    "explanation": "Silently dropping it means a possibly accidental change to payment resilience ships with no one asked. False-positive discipline applies to asserted defects; a clearly labelled question is not a false positive, and this policy would hide exactly the class of change humans most want to see."
   },
   {
    "letter": "C",
    "text": "Rate it \"medium\" rather than \"critical\" so it appears in the review but does not block the merge, letting severity encode the uncertainty.",
    "correct": false,
    "explanation": "Using severity to express confidence corrupts the scale: a medium finding should mean medium impact, not \"we are not sure\". Developers then cannot trust severity for gating, and the reviewer still asserts a defect it cannot substantiate."
   },
   {
    "letter": "D",
    "text": "Have the reviewer search the tracker and git history with additional tools until it can classify the change, then post a finding.",
    "correct": false,
    "explanation": "Extra evidence gathering is reasonable, but the stem says there is no ticket and the description is uninformative, so more searching will not reveal intent. It still ends in a forced classification, at higher cost and latency per PR."
   }
  ]
 },
 {
  "scenarioTitle": "Claude Code for Continuous Integration",
  "domain": "Agentic Architecture",
  "prompt": "Your CI test-generation step reports that it raised line coverage on the <code>billing</code> module from 61% to 79% over six weeks, and leadership wants to expand it to all services. A staff engineer samples 30 generated tests and finds that 18 either assert only that a function returns without throwing or snapshot the current output; a known rounding bug in <code>applyDiscount</code> is exercised by three of them and caught by none. How should the step's value be measured before expansion?",
  "options": [
   {
    "letter": "A",
    "text": "Require a minimum coverage gain per PR and track the module's coverage trend over time, since coverage is the metric the step was commissioned to move.",
    "correct": false,
    "explanation": "Coverage measures lines executed, not behaviour verified, which is exactly how 18 of 30 tests can raise it while catching nothing. Making the target a coverage gain rewards the agent for more of the same and hides the rounding bug behind a green number."
   },
   {
    "letter": "B",
    "text": "Ask a second Claude session to grade each generated test for meaningfulness on a 1 to 5 scale and report the average grade per module.",
    "correct": false,
    "explanation": "A model grading another model's tests for \"meaningfulness\" produces a plausible-looking score with no ground truth, and shares the generator's blind spots about what the code should do. It measures agreement between two Claude sessions, not defect detection."
   },
   {
    "letter": "C",
    "text": "Run mutation testing on the covered modules and measure how many injected faults the generated tests kill, compared with the existing suite.",
    "correct": true,
    "explanation": "The question is whether the generated tests detect faults, and mutation testing answers it directly: inject changes into the code and count how many the tests catch. Comparing kill rate with the existing suite shows the marginal value of the step, and the <code>applyDiscount</code> bug would surface as surviving mutants. It evaluates the outcome the step is meant to produce rather than a proxy."
   },
   {
    "letter": "D",
    "text": "Count assertions per generated test and reject tests with fewer than two, so trivially passing tests are filtered out before they are committed.",
    "correct": false,
    "explanation": "An assertion count is a proxy that is easy to satisfy without value: two <code>toBeDefined</code> checks pass the filter and a single precise equality assertion fails it. Snapshot tests carry one assertion that encodes the current, possibly buggy, output."
   }
  ]
 },
 {
  "scenarioTitle": "Code Generation with Claude Code",
  "domain": "Agentic Architecture",
  "prompt": "A developer wants Claude Code to migrate 60 files from a deprecated <code>moment</code> API to <code>date-fns</code> overnight, unattended. The mapping is mechanical and documented in an internal guide. They propose running <code>claude --dangerously-skip-permissions</code> in their main working tree so nothing prompts them. How should the run be bounded?",
  "options": [
   {
    "letter": "A",
    "text": "Split the migration into 60 short interactive sessions, one file each with the default permission prompts, so that any single mistake stays contained to one file.",
    "correct": false,
    "explanation": "Sixty interactive sessions is not unattended, which was the whole point, and per-file sessions lose the shared understanding of the mapping that makes the migration consistent. Containment is real but bought by discarding the automation."
   },
   {
    "letter": "B",
    "text": "Accept skipping permissions but instruct in the prompt to touch only files that import <code>moment</code> and to run the test suite after each file.",
    "correct": false,
    "explanation": "Prompt instructions are a request, not a boundary; with permissions skipped in the main working tree, a misread instruction can modify or delete anything. The test run helps detect problems but nothing limits what an unattended session may do."
   },
   {
    "letter": "C",
    "text": "Run it in a dedicated git worktree with a permission allowlist for <code>Edit</code> and the test command, and use the resulting PR as the human review gate.",
    "correct": true,
    "explanation": "The worktree isolates the blast radius, the allowlist limits the agent to the actions the task needs without prompting, and the PR puts a human at the point where judgment matters. This gives an unattended run with enforced limits, which the other options either lack or achieve by removing the automation."
   },
   {
    "letter": "D",
    "text": "Run it with permissions skipped inside a plan-mode session first, then replay the approved plan in a second unattended session using the same flag.",
    "correct": false,
    "explanation": "A plan does not constrain execution; the second session is still unrestricted in the main tree and may deviate from the plan when files differ from expectations. Plan mode also does not need permissions skipped, since it does not write, so the first step adds nothing."
   }
  ]
 },
 {
  "scenarioTitle": "Code Generation with Claude Code",
  "domain": "Agentic Architecture",
  "prompt": "While fixing a failing test in <code>OrderTotalTest</code>, Claude Code changed the expected value from 119.00 to 118.99 and reported success. The test was right; the rounding bug is in the code. The team wants Claude to keep fixing failing tests autonomously but never \"fix\" a test by changing what it asserts. Which stop condition belongs in the workflow?",
  "options": [
   {
    "letter": "A",
    "text": "Make failing tests the contract: if the only way to pass is changing an assertion, Claude stops and reports the conflict instead of editing.",
    "correct": true,
    "explanation": "This defines the exact boundary that was crossed and turns it into an explicit stop-and-report condition, so the agent stays autonomous on genuine code fixes and escalates only when the test and the code disagree. It preserves the ability to add or repair tests while making assertion changes a human decision."
   },
   {
    "letter": "B",
    "text": "Add a hook that blocks all edits to files under <code>tests/</code>, guaranteeing that test assertions can never be altered by the agent on its own.",
    "correct": false,
    "explanation": "A blanket block stops legitimate work too: writing new tests, fixing a genuinely broken fixture, or updating a test after an intended behaviour change. The boundary is about intent, whether the code or the expectation is wrong, and a path rule cannot express that."
   },
   {
    "letter": "C",
    "text": "Require Claude to run the full suite after every change and revert whenever the pass count decreases, so regressions cannot land.",
    "correct": false,
    "explanation": "The pass count increased here, so this check would have approved the bad edit. It guards against a different failure, breaking other tests, and gives no signal when an assertion is weakened to match buggy code."
   },
   {
    "letter": "D",
    "text": "Move test fixing into plan mode so the developer reviews and approves each proposed change before Claude applies it to the code.",
    "correct": false,
    "explanation": "Approving every fix removes the autonomy the team asked to keep and makes routine code fixes as expensive as the rare contested one. It solves the problem by putting a human on every decision rather than on the specific decision that needs one."
   }
  ]
 },
 {
  "scenarioTitle": "Code Generation with Claude Code",
  "domain": "Agentic Architecture",
  "prompt": "Your team needs 12 new REST endpoints that all follow the pattern of the existing <code>orders</code> handler: validation, service call, error mapping, OpenAPI annotation. A developer proposes launching 12 subagents in parallel, one per endpoint, to finish in minutes. In a trial the results compiled but used three different error-mapping styles and two annotation formats. What approach fits?",
  "options": [
   {
    "letter": "A",
    "text": "Keep the parallel fan-out but give every subagent the full <code>CLAUDE.md</code> and a longer prompt describing the conventions in detail.",
    "correct": false,
    "explanation": "The divergence came from twelve independent inferences of the pattern, and prose conventions still leave room for twelve interpretations. More text raises cost per subagent without providing the one thing that would align them: a concrete, already-reviewed example to copy."
   },
   {
    "letter": "B",
    "text": "Launch the 12 subagents sequentially instead of in parallel so each can see the previous endpoint's output before generating its own.",
    "correct": false,
    "explanation": "Subagents do not inherit each other's context, so running them in sequence gives none of them the previous output unless it is passed explicitly. This loses the speed of fan-out while keeping the inconsistency, the worst of both."
   },
   {
    "letter": "C",
    "text": "Have one subagent generate all 12 endpoints in a single run so a single context holds the pattern, then review the whole batch.",
    "correct": false,
    "explanation": "A single context does improve consistency, but there is no checkpoint: if the pattern is misread on the first endpoint, all twelve are wrong the same way and the review lands on a large batch. Consistency without an early human check just makes errors uniform."
   },
   {
    "letter": "D",
    "text": "Generate one endpoint first, review it, then hand the reviewed file to the remaining runs as the exemplar to copy.",
    "correct": true,
    "explanation": "One reviewed exemplar anchors every subsequent generation to a concrete, approved artifact rather than to inferred conventions, so parallel runs stay consistent. It keeps the speed of fan-out, puts the human check where it is cheapest, and beats prose instructions because the model copies rather than interprets."
   }
  ]
 },
 {
  "scenarioTitle": "Code Generation with Claude Code",
  "domain": "Agentic Architecture",
  "prompt": "Claude Code has spent 25 minutes on an intermittent <code>ECONNRESET</code> in an integration test. The transcript shows it alternating between two hypotheses: it adds a connection-pool timeout, the test still fails, it reverts and adds a retry wrapper, the test still fails, and it returns to the timeout. No hypothesis has been checked against evidence. How do you break the loop?",
  "options": [
   {
    "letter": "A",
    "text": "Run <code>/compact</code> to clear the accumulated failed attempts from context so Claude can approach the problem fresh.",
    "correct": false,
    "explanation": "Context pressure is not what drives this loop; the agent is editing before verifying. Compacting also blurs the record of what was already tried, which is the one useful thing the transcript contains, so the next attempt may repeat the same two edits."
   },
   {
    "letter": "B",
    "text": "Stop and require a reproduction first: make the failure deterministic, then state a hypothesis with evidence before any edit.",
    "correct": true,
    "explanation": "The loop exists because edits are being used as experiments against an intermittent failure, so no result confirms or refutes anything. Forcing reproduce, then hypothesis with evidence, then edit gives each step a verifiable outcome. It fixes the loop's structure rather than its inputs or its context."
   },
   {
    "letter": "C",
    "text": "Delegate the investigation to a fresh subagent with the same task description so an uncontaminated context can find the fix.",
    "correct": false,
    "explanation": "A new context running the same unstructured process will oscillate the same way, and it discards what has been learned. Delegation helps when the parent's context is the problem, not when the debugging method is."
   },
   {
    "letter": "D",
    "text": "Paste the full server logs and the pool library's source into the conversation so Claude has all the information it could need.",
    "correct": false,
    "explanation": "Missing information is not the failure mode; the agent never tested the information it had. A large paste fills context, pushes the transcript toward compaction, and still leaves the model free to guess-and-edit."
   }
  ]
 },
 {
  "scenarioTitle": "Code Generation with Claude Code",
  "domain": "Agentic Architecture",
  "prompt": "Legal requires a new license header in every one of 3,100 source files across the monorepo, replacing three older header variants. A developer asks Claude Code to \"update the headers\" and watches it Read and Edit files one at a time; after 40 minutes it has finished 180 files and the session context is filling. What should the developer do?",
  "options": [
   {
    "letter": "A",
    "text": "Ask Claude to write and test a script that performs the replacement, run it, then use Claude to review the diff for edge cases.",
    "correct": true,
    "explanation": "The task is deterministic and repetitive, so the right agent output is a program, not 3,100 edits. A script finishes in seconds, applies one rule uniformly, and leaves the model for the judgment work: writing the script correctly and reviewing the odd files the script mishandled."
   },
   {
    "letter": "B",
    "text": "Split the remaining files across eight parallel subagents by directory so the same approach completes within the session limit.",
    "correct": false,
    "explanation": "Parallelism multiplies the cost of using a model as a text-replacement engine and invites eight slightly different interpretations of the header rules. It finishes sooner but is still the wrong tool for a mechanical transformation."
   },
   {
    "letter": "C",
    "text": "Run <code>/compact</code> at intervals and continue, since the task is mechanical and Claude has demonstrated it handles it correctly.",
    "correct": false,
    "explanation": "Compaction addresses context, not throughput: at the observed rate the remaining files take roughly eleven more hours of model turns. The task being mechanical is the argument against doing it turn by turn, not for it."
   },
   {
    "letter": "D",
    "text": "Switch to plan mode so Claude lists every affected file and the exact replacement, then approve the plan and execute it.",
    "correct": false,
    "explanation": "A plan enumerating 3,100 files is not reviewable, and executing it still means per-file edits at the same pace. Plan mode helps when the approach is uncertain; here the approach is obvious and the problem is the execution mechanism."
   }
  ]
 },
 {
  "scenarioTitle": "Developer Productivity with Claude",
  "domain": "Agentic Architecture",
  "prompt": "You automated weekly dependency updates with an SDK agent that has Bash and Edit: it bumps versions, runs tests, and opens a PR that auto-merges when green. Last week it bumped an ORM from 5.x to 6.x; tests passed because the removed API was only exercised by production jobs, and two nightly jobs failed. What autonomy boundary would you set?",
  "options": [
   {
    "letter": "A",
    "text": "Require the agent to read each package's changelog before bumping and skip any update whose notes mention breaking changes.",
    "correct": false,
    "explanation": "This leaves the decision with the model and depends on changelogs being complete and clearly worded, which many are not. The agent remains fully autonomous over major upgrades; it is just asked to be more careful, which is not a boundary."
   },
   {
    "letter": "B",
    "text": "Remove auto-merge entirely so every dependency PR waits for a human reviewer, since the incident shows tests cannot catch every regression.",
    "correct": false,
    "explanation": "Most weekly bumps are patch releases where a green run is adequate evidence; forcing humans to review all of them creates queue fatigue and rubber-stamping. It responds to one risky class of change by giving up automation on every class."
   },
   {
    "letter": "C",
    "text": "Have the agent also run the nightly job suite before merging, closing the gap that let the ORM change through.",
    "correct": false,
    "explanation": "Extending coverage helps for this specific gap but the next major upgrade will find another one; test coverage is never a proof of compatibility. It also makes every bump slow and expensive when the concern applies only to majors."
   },
   {
    "letter": "D",
    "text": "Tier by risk: patch and minor bumps may auto-merge on green; major bumps open a PR with a migration summary and require human approval.",
    "correct": true,
    "explanation": "Semver already encodes the risk signal, so tiering puts the human exactly where breaking changes are expected and nowhere else. Routine bumps stay automatic, majors get a reviewed PR with context, and the boundary is enforced by version, not by the model's reading of a changelog."
   }
  ]
 },
 {
  "scenarioTitle": "Developer Productivity with Claude",
  "domain": "Agentic Architecture",
  "prompt": "Your codebase-exploration assistant fans every question out to six subagents (API, data, jobs, UI, infra, tests) and synthesizes their reports. Engineers like it for \"how does checkout work end to end?\" but complain that \"what does <code>normalizeSku</code> return?\" takes 90 seconds, costs about 40x a direct lookup, and the synthesis sometimes contradicts the one relevant subagent. What should change?",
  "options": [
   {
    "letter": "A",
    "text": "Keep the fan-out but cut each subagent's turn budget so simple questions return faster and cost less per question.",
    "correct": false,
    "explanation": "Six agents still run for a question one Grep answers, and the synthesis step that introduces contradictions is untouched. Smaller budgets also degrade the broad questions the engineers value, so it makes both cases worse to make one slightly cheaper."
   },
   {
    "letter": "B",
    "text": "Let the main agent answer directly with Grep and Read by default, and fan out to subagents only for questions spanning multiple areas.",
    "correct": true,
    "explanation": "Delegation should be proportional to the question. A single-symbol lookup is one tool call in the main context, with no synthesis to go wrong; cross-cutting questions still get the parallel exploration that works. This removes the overhead and the contradiction source for simple cases without giving up the multi-agent path."
   },
   {
    "letter": "C",
    "text": "Add a seventh \"router\" subagent that reads each question and picks which of the six should run, so irrelevant reports are never produced.",
    "correct": false,
    "explanation": "Routing removes irrelevant reports but keeps a full delegation round-trip plus synthesis for a question the main agent could answer itself. It adds a model call and latency in front of every question, including the trivial ones."
   },
   {
    "letter": "D",
    "text": "Replace the six area subagents with one general explorer subagent so every question yields exactly one report for the coordinator to synthesize.",
    "correct": false,
    "explanation": "One subagent is still an indirection for trivial lookups, and for the end-to-end questions it loses the parallel coverage that made the tool useful. It trades the working case for a modest improvement on the failing one."
   }
  ]
 },
 {
  "scenarioTitle": "Developer Productivity with Claude",
  "domain": "Agentic Architecture",
  "prompt": "Before expanding your codebase Q&A agent from one team to the whole engineering org, leadership asks for proof it works. The proposed metric is median time-to-answer, currently 40 seconds, plus a thumbs-up button. In a spot check, 3 of 15 confident answers pointed to functions that no longer exist after a refactor, and all three had a thumbs-up. How should agent quality be evaluated?",
  "options": [
   {
    "letter": "A",
    "text": "Add \"cite file and line for every claim\" to the system prompt and track what share of answers include citations as the quality metric.",
    "correct": false,
    "explanation": "Citations improve answers but their presence is not correctness; the three wrong answers could each have cited a stale location. Measuring citation rate rewards the form of grounding without checking whether the grounding is true."
   },
   {
    "letter": "B",
    "text": "Replace the thumbs-up with a five-point rating and require a comment, so engineers provide richer signal on each answer.",
    "correct": false,
    "explanation": "Engineers rated the wrong answers positively because they did not know the code; a finer scale asks the same uninformed judge for more precision. It adds friction and still cannot detect confident errors in unfamiliar code."
   },
   {
    "letter": "C",
    "text": "Build a set of real questions with verified answers in the current repo, grade the agent against it, and re-run after prompt or model changes.",
    "correct": true,
    "explanation": "Only a ground-truth set can tell a confident wrong answer from a right one, which is exactly what the spot check found and the thumbs-up missed. It also makes quality a regression test, so later prompt or model changes are checked rather than assumed. Speed and satisfaction can be tracked alongside, not instead."
   },
   {
    "letter": "D",
    "text": "Log every tool call and have a second Claude session judge whether the agent explored enough of the repository before it answered each question.",
    "correct": false,
    "explanation": "Exploration depth is a process metric with no ground truth; an agent can read many files and still cite a deleted function. A model judge without verified answers reproduces the original problem one level up."
   }
  ]
 },
 {
  "scenarioTitle": "Developer Productivity with Claude",
  "domain": "Agentic Architecture",
  "prompt": "You built an agent that tidies the issue tracker weekly through MCP tools (<code>list_issues</code>, <code>add_comment</code>, <code>close_issue</code>): it closes issues with no activity for 90 days. Last run it closed 212 issues, including 9 whose latest comment said \"keep open, blocked on vendor\" and 2 flagged as compliance items. Closing is reversible, but notifications already went to 60 stakeholders. What should change?",
  "options": [
   {
    "letter": "A",
    "text": "Make the run produce a proposed action list for a human to approve, then execute only the approved closures in a second step.",
    "correct": true,
    "explanation": "Bulk actions with external side effects are where a human gate pays for itself: reviewing a list of 212 candidates takes minutes, while unwinding notifications to 60 people cannot be done. Splitting propose from execute keeps the agent doing the work and the human doing the judgment, and it covers exclusion cases nobody has written down yet."
   },
   {
    "letter": "B",
    "text": "Add \"do not close issues whose comments ask to keep them open or that carry compliance labels\" to the agent's instructions.",
    "correct": false,
    "explanation": "This patches the two failure categories that were noticed and relies on the model applying them reliably. The next unwritten exception, an issue referenced by a live incident for example, leaks the same way, and the run still acts before anyone looks."
   },
   {
    "letter": "C",
    "text": "Have the agent add a warning comment and wait a week before closing, giving stakeholders a chance to object before the action.",
    "correct": false,
    "explanation": "Two hundred warning comments are themselves a mass notification, and the design shifts the review burden to every stakeholder rather than one operator. Issues nobody watches, which is most stale issues, still close without anyone deciding."
   },
   {
    "letter": "D",
    "text": "Lower the blast radius by capping each run at 25 closures, so a bad run affects fewer issues and can be reverted quickly.",
    "correct": false,
    "explanation": "A cap slows the damage but does not prevent it: the same wrong closures happen over nine weeks instead of one, each with its notifications. It also leaves the backlog growing while offering no mechanism for a person to catch the mistakes."
   }
  ]
 },
 {
  "scenarioTitle": "Developer Productivity with Claude",
  "domain": "Agentic Architecture",
  "prompt": "Your team built a weekly changelog agent: given Bash, Grep, and a GitHub MCP server, it is told to \"gather last week's merged PRs, summarize them, and post to Slack\". Some weeks it uses <code>git log</code> and misses squash-merge descriptions; other weeks it queries the MCP server and includes unmerged PRs; twice it posted before finishing. The summaries themselves are good. What should the design be?",
  "options": [
   {
    "letter": "A",
    "text": "Give the agent a detailed procedure in its system prompt: query the MCP server, filter by merged state and date, summarize, then post.",
    "correct": false,
    "explanation": "A procedure in the prompt is still executed by a model that may choose differently each week, which is the observed problem. It narrows the variance but the gathering step remains probabilistic when it has a single correct answer."
   },
   {
    "letter": "B",
    "text": "Split into a gathering subagent and a writing subagent so each has a narrow job and the coordinator checks the PR list before posting.",
    "correct": false,
    "explanation": "Adding agents to do deterministic work multiplies cost and coordination without removing the variability; the gathering subagent can still pick <code>git log</code>. The coordinator check is itself another model judgment about a list that code can produce exactly."
   },
   {
    "letter": "C",
    "text": "Add a <code>PreToolUse</code> hook on the Slack post tool that verifies the message references at least one PR before it is sent.",
    "correct": false,
    "explanation": "A hook can stop an empty or premature post but cannot know whether the PR set is complete or excludes unmerged work, which is where most of the errors came from. It guards one symptom and leaves the variability in the gathering step in place."
   },
   {
    "letter": "D",
    "text": "Fetch and filter the merged PRs in a script, call Claude once to write the summary, and post from the script; no agent loop.",
    "correct": true,
    "explanation": "The only step that benefits from a model is writing the summary, and that step already works. Gathering and posting have exact, testable definitions, so code does them the same way every week and cannot post early. Using an agent loop here bought variability and paid for it in cost and trust."
   }
  ]
 },
 {
  "scenarioTitle": "Developer Productivity with Claude",
  "domain": "Agentic Architecture",
  "prompt": "Engineers stopped using your SDK-based assistant after a week. Traces show it asked for confirmation on every action: 37 approval prompts in one session, 31 of them for Read, Grep, and Glob calls. The one time it ran <code>git push --force</code> to a shared branch it also asked, and the engineer, numbed by then, approved it. How should the permission design change?",
  "options": [
   {
    "letter": "A",
    "text": "Turn on auto-approval for all tools and rely on the git worktree isolation you already use so mistakes can be discarded.",
    "correct": false,
    "explanation": "A worktree contains local edits, but a force push leaves it and rewrites a shared remote; that is exactly the action that needs a deliberate yes. Auto-approving everything fixes the fatigue by removing the one prompt that mattered."
   },
   {
    "letter": "B",
    "text": "Auto-approve read-only tools and reserve confirmation for actions that change shared or external state, such as pushes and deletions.",
    "correct": true,
    "explanation": "Prompts should track reversibility, not tool count. Reads cannot cause harm, so approving them buys nothing and spends the engineer's attention; pushes and deletions are rare and consequential, so a prompt there is noticed. This makes the remaining confirmations meaningful, which is what makes them work."
   },
   {
    "letter": "C",
    "text": "Keep confirmation for every tool but batch the prompts so the engineer approves an entire plan at once rather than one call at a time.",
    "correct": false,
    "explanation": "Batching hides the dangerous call in a bundle of harmless ones, so the force push gets approved with the reads around it. It reduces clicks while making the one important decision less visible than before."
   },
   {
    "letter": "D",
    "text": "Ask for confirmation only on the first use of each tool per session, then remember the decision for the rest of the session.",
    "correct": false,
    "explanation": "The first Bash call is usually <code>git status</code>; approving it would silently approve the later force push. Per-tool memory conflates harmless and destructive uses of the same tool, which is precisely the distinction that matters."
   }
  ]
 },
 {
  "scenarioTitle": "Structured Data Extraction",
  "domain": "Agentic Architecture",
  "prompt": "Reviewers correct about 300 extracted records a day and the corrections are logged. An engineer proposes a nightly agent that reads the day's corrections, rewrites the extraction prompt and few-shot examples to address them, and deploys the new prompt before the morning batch, arguing that prompt edits are low-risk text changes. The pipeline processes 20,000 invoices a day into the ERP. How should this agent's autonomy be bounded?",
  "options": [
   {
    "letter": "A",
    "text": "Allow automatic deployment only when the rewrite changes fewer than 20 lines of the prompt, and route larger rewrites to an engineer for review.",
    "correct": false,
    "explanation": "Size is not risk: a one-word change to a field definition can shift 20,000 extractions a day, while a long rewrite of examples may be harmless. A line threshold gives the appearance of a boundary without measuring the thing that matters, which is the effect on extraction quality."
   },
   {
    "letter": "B",
    "text": "Deploy automatically but keep the previous prompt available, and roll back if the next day's reviewer overturn rate rises above the trailing average.",
    "correct": false,
    "explanation": "This uses a day of production as the test bed: a regressed prompt sends wrong records into the ERP before the signal arrives, and overturn rate lags because reviewers see only the sampled share of records. Rollback limits the duration of damage, not its occurrence."
   },
   {
    "letter": "C",
    "text": "Let the agent propose changes and score them on the labelled evaluation set; a human promotes a prompt only when scores hold or improve.",
    "correct": true,
    "explanation": "Generating candidate prompt changes from corrections is a good use of the agent; deploying them is a production change to a system feeding an ERP. Scoring each candidate against the labelled evaluation set turns the decision into evidence, and requiring a human to promote keeps the deployment boundary where the consequence lives. The agent does the work; it does not own the release."
   },
   {
    "letter": "D",
    "text": "Do not automate it; have engineers rewrite the prompt monthly from a summary of corrections, since prompt changes need human judgement.",
    "correct": false,
    "explanation": "Refusing the automation throws away the useful part, which is turning 300 daily corrections into concrete candidate fixes, and lets systematic errors persist for weeks. The concern is real but is addressed by gating the deployment, not by removing the agent."
   }
  ]
 },
 {
  "scenarioTitle": "Structured Data Extraction",
  "domain": "Agentic Architecture",
  "prompt": "Your extraction agent submits validated invoices to the ERP with <code>submit_record</code> and also holds a <code>create_vendor</code> tool intended for genuinely new suppliers. When the ERP rejects a submission with <code>vendor_not_found</code>, traces show the agent calling <code>create_vendor</code> with the name as extracted and resubmitting successfully. Finance found 43 duplicate vendor records in a month, such as \"ACME Corp.\" beside \"Acme Corporation\", each with its own payment history. What should change?",
  "options": [
   {
    "letter": "A",
    "text": "Remove <code>create_vendor</code> from the agent's tools; on <code>vendor_not_found</code> it stops and routes the record to a human with the extracted name and the closest matches.",
    "correct": true,
    "explanation": "Creating master data is a business decision with lasting consequences, and the agent was given a tool that let it treat an ERP rejection as an obstacle to route around. Removing the tool makes the rejection a stop condition: the agent hands over the evidence a human needs to pick the existing vendor or create a new one deliberately. Genuinely new suppliers are rare enough to be handled that way."
   },
   {
    "letter": "B",
    "text": "Keep the tool but instruct the agent to call <code>create_vendor</code> only after confirming that no vendor with a similar name already exists in the master list.",
    "correct": false,
    "explanation": "A prompt rule leaves the decision with the model and depends on it judging name similarity reliably every time; \"ACME Corp.\" versus \"Acme Corporation\" is exactly the judgement it already got wrong. The tool that caused the damage stays available whenever the rule is not followed."
   },
   {
    "letter": "C",
    "text": "Make <code>create_vendor</code> perform a server-side fuzzy match and return existing candidates instead of creating when similarity exceeds 0.8, so duplicates are caught in the tool.",
    "correct": false,
    "explanation": "Fuzzy matching in the tool catches some duplicates, but a threshold will still split vendors with different legal names and merge distinct ones, and the agent remains empowered to create records whenever the match falls below it. It hardens the tool without asking whether the agent should hold it."
   },
   {
    "letter": "D",
    "text": "Add a second Claude call that must approve every <code>create_vendor</code> request by reviewing the extracted name against the vendor list before the tool executes.",
    "correct": false,
    "explanation": "A second model call adds cost and a false sense of a checkpoint: it reads the same extracted name with the same knowledge gaps and no ground truth about whether the supplier is new. Model-approves-model is not a human in the loop, and duplicates below its notice still get created."
   }
  ]
 },
 {
  "scenarioTitle": "Structured Data Extraction",
  "domain": "Agentic Architecture",
  "prompt": "Your extraction agent holds both extraction tools and a <code>submit_to_erp</code> tool in one loop. When the ERP returns a transient 503, the agent sometimes re-extracts the document, gets a slightly different record, and submits again; the ERP now holds 140 near-duplicate purchase orders from one week. Retrying is appropriate; duplicates are not. How should the boundary be drawn?",
  "options": [
   {
    "letter": "A",
    "text": "Instruct the agent to retry <code>submit_to_erp</code> with the identical record on a 503 and never re-run extraction after a submission attempt.",
    "correct": false,
    "explanation": "This describes the right behaviour but leaves enforcement to the model inside the same loop that produced the duplicates. Under an error it may still reason its way back to extraction, and there is nothing structural to stop the second submission."
   },
   {
    "letter": "B",
    "text": "End the agent's job at a validated record; have deterministic code submit it with an idempotency key and handle retries.",
    "correct": true,
    "explanation": "Submission is a side-effecting operation with a known retry policy, so it belongs in code that retries the same payload under one idempotency key. The agent's competence is producing a correct record; stopping it there removes the path from a 503 back to re-extraction entirely, rather than asking it to avoid that path."
   },
   {
    "letter": "C",
    "text": "Make <code>submit_to_erp</code> idempotent by hashing the record contents so identical resubmissions are rejected by the ERP.",
    "correct": false,
    "explanation": "The duplicates were not identical; re-extraction produced slightly different records, so content hashes differ and pass. Idempotency keyed on content protects against exact repeats, not against the agent regenerating the record."
   },
   {
    "letter": "D",
    "text": "Add a <code>check_existing_order</code> tool the agent must call before submitting so it can detect a duplicate it created earlier.",
    "correct": false,
    "explanation": "Matching near-duplicates is itself a judgment call the agent can get wrong, and it adds turns to every document to defend against a problem created by letting the agent own submission. It treats the symptom while keeping the loop that causes it."
   }
  ]
 },
 {
  "scenarioTitle": "Structured Data Extraction",
  "domain": "Agentic Architecture",
  "prompt": "Your extraction agent can call <code>route_to_human</code> when a document is ambiguous. Operations set a target of escalating no more than 5% of documents. The agent escalated 11%, a prompt tweak brought it to 4%, and the team is ready to ship. A reviewer notes that in the 4% run, two contracts with contradictory termination clauses went straight to the downstream system. How should escalation behaviour be evaluated?",
  "options": [
   {
    "letter": "A",
    "text": "Set the target as a range, for example 4 to 8 percent, so the prompt cannot be tuned so low that ambiguous documents slip through unnoticed.",
    "correct": false,
    "explanation": "A rate says nothing about which documents were escalated. An agent can sit inside any range while escalating easy documents and passing contradictory ones; the range makes the number look considered without measuring the decisions behind it."
   },
   {
    "letter": "B",
    "text": "Sample 50 escalated documents weekly and count how many reviewers judged genuinely ambiguous, driving the rate down only while that share stays high.",
    "correct": false,
    "explanation": "This measures escalation precision, whether escalated documents deserved it, and is worth doing. But the reported failure is a miss: documents that should have been escalated and were not, which a sample drawn only from escalations can never observe."
   },
   {
    "letter": "C",
    "text": "Label a document set for whether escalation was warranted, then measure both unnecessary and missed escalations against it before tuning the prompt.",
    "correct": true,
    "explanation": "Escalation is a classification decision and needs both error types measured: over-escalation costs reviewer time, under-escalation lets contradictory contracts through. A labelled set exposes the trade the prompt tweak actually made and lets the team tune toward correct decisions instead of toward a rate."
   },
   {
    "letter": "D",
    "text": "Track downstream correction rates: if the systems receiving the records report few manual fixes, the escalation rate is low enough to ship.",
    "correct": false,
    "explanation": "Downstream corrections are a lagging and partial signal; a contradictory termination clause may not surface until a dispute months later, and many errors are never traced back. It also cannot tell an escalation miss from an extraction error."
   }
  ]
 }
]